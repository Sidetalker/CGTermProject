#ifndef TEXTURE_DEFS_H
#define TEXTURE_DEFS_H

// contains the textures
struct TextureTypes
{
	enum id
	{
		BULLSEYE,
		GRID,
		WALL,
        TITLE,
		
		NUM_TEXTURES,
		INVALID_TEXTURE
	};
};

#endif