#ifndef H_GLOBALS
#define H_GLOBALS

// forward declaration of classes (to prevent cyclic dependencies)
class Game;
class Textures;

// game pointer
extern Game* game;
extern Textures* textures;

#endif